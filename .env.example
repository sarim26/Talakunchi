# Copy to .env and fill in values (do NOT commit .env)

# AI provider mode: mock | gemini
AI_MODE=gemini

# Gemini (Google AI Studio / Gemini API)
GEMINI_API_KEY=
# If you get "model not found", call /api/ai/models to see allowed values.
GEMINI_MODEL=gemini-1.5-flash-latest

